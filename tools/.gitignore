# runtime artifacts — not source code
*/projects/
*/data/
*/context.md
*.key

# build artifacts (8-10MB each; build from source via `go build`)
agent-db/agent-db
agent-log/agent-log
agent-explorer/agent-explorer

# runtime index artifacts (codebase-memory graph cache, regenerated)
*/.codebase-memory/
